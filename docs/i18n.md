# Локализация (i18n)

## Обзор

Файлы локализации находятся в директории `functions/src/i18n/` и содержат все пользовательские строки для MamaBot Telegram бота. Каждый файл представляет один язык и имеет идентичную структуру ключей.

Языковые коды (`ru`, `en`) соответствуют полю `language` в коллекции `users` (`'ru' | 'en'`).

---

## Файлы

| Файл | Язык |
|------|------|
| `functions/src/i18n/ru.json` | Русский |
| `functions/src/i18n/en.json` | Английский |

---

## Структура ключей

Ключи используют dot-нотацию с сегментами в snake_case. Вложенные объекты разрешаются через точку, например:

```
onboarding.welcome         → "👋 Добро пожаловать..."
nutrition.meal_types.breakfast → "🌅 Завтрак"
```

### Топ-уровневые категории

| Категория           | Описание                              |
|---------------------|---------------------------------------|
| `onboarding`        | Экран приветствия и регистрации       |
| `menu`              | Главное меню и кнопки навигации       |
| `common`            | Общие кнопки и сообщения              |
| `week_info`         | Информация о неделе беременности      |
| `mood`              | Дневник настроения                    |
| `nutrition`         | Трекер питания                        |
| `notifications`     | Уведомления                           |

### Ключи онбординга (onboarding)

| Ключ | Описание | Пример (RU) |
|------|----------|-------------|
| `onboarding.welcome` | Приветствие | 👋 Добро пожаловать в MamaBot... |
| `onboarding.choose_language` | Выбор языка | 🌍 Выбери язык: |
| `onboarding.language_ru` | Кнопка русского | 🇷🇺 Русский |
| `onboarding.language_en` | Кнопка английского | 🇬🇧 English |
| `onboarding.language_saved` | Язык сохранён | ✅ {{lang}} язык установлен! |
| `onboarding.already_registered` | Уже зарегистрирован | 👋 Ты уже зарегистрирован(а)! |
| `onboarding.ask_lmp` | Запрос даты ПМ | 📅 Введи дату первого дня... |
| `onboarding.lmp_format_hint` | Подсказка формата | Пожалуйста, используй формат... |
| `onboarding.invalid_date` | Ошибка формата даты | ❌ Неверный формат даты. |
| `onboarding.date_in_future` | Дата в будущем | ❌ Дата не может быть в будущем. |
| `onboarding.date_too_old` | Дата старше 42 нед | ⚠️ Эта дата старше 42 недель. |
| `onboarding.week_calculated` | Неделя рассчитана | 🤰 Ты на {{week}} неделе! |
| `onboarding.edd_confirm` | Подтверждение ПДР | 📅 ПДР: {{edd}}. Верно? |
| `onboarding.edd_correct` | Кнопка «Верно» | ✅ Верно |
| `onboarding.edd_edit` | Кнопка «Исправить» | ✏️ Исправить |
| `onboarding.edd_confirmed` | ПДР подтверждена | ✅ ПДР {{edd}} сохранена! |
| `onboarding.edd_prompt` | Запрос нового ПДР | 📅 Введи предполагаемую дату родов... |
| `onboarding.edd_invalid_format` | Ошибка формата ПДР | ❌ Неверный формат даты. |
| `onboarding.edd_before_lmp` | ПДР раньше ПМ | ❌ ПДР не может быть раньше... |
| `onboarding.edd_too_late` | ПДР слишком далеко | ❌ ПДР слишком далеко... |
| `onboarding.edd_saved` | ПДР сохранена | ✅ ПДР {{edd}} сохранена! |

### Плейсхолдеры (переменные)

Строки могут содержать плейсхолдеры в фигурных скобках:

| Плейсхолдер | Описание                         | Пример                           |
|-------------|----------------------------------|----------------------------------|
| `{week}`    | Номер недели беременности        | `"{week} неделя беременности"`   |
| `{date}`    | Дата в формате YYYY-MM-DD        | `"Дата сохранена: {date}"`       |
| `{code}`    | Код-приглашение для партнёра     | `"Твой код: {code}"`             |
| `{{edd}}`   | ПДР в формате ДД.ММ.ГГГГ         | `"ПДР {{edd}} сохранена!"`       |
| `{{lmp}}`   | Дата ПМ в формате ДД.ММ.ГГГГ     | `"ПДР не может быть раньше ({{lmp}})."` |

---

## Конвенция именования ключей

1. **Все ключи в snake_case** — например, `baby_development`, `meal_types`, `vitamin_recommendations`.
2. **Топ-уровневые категории** — существительные в единственном числе: `menu`, `mood`, `nutrition`.
3. **Вложенные объекты** — для группировки связанных ключей: `meal_types.breakfast`, `meal_types.lunch`.
4. **Единообразие** — одинаковые ключи в обоих языковых файлах; каждый ключ присутствует в `ru.json` и `en.json`.

---

## Как добавить новый ключ

1. Добавьте ключ в `ru.json` с русским переводом.
2. Добавьте **точно такой же ключ** в `en.json` с английским переводом.
3. Если ключ относится к новой категории, создайте новый топ-уровневый объект.
4. Запустите тесты для проверки паритета ключей:
   ```bash
   cd functions
   npx vitest run src/i18n/
   ```
5. Тесты проверят:
   - Оба файла существуют и содержат валидный JSON
   - Все ключи (включая вложенные) идентичны в обоих файлах
   - Нет пустых строк
   - `ru.json` содержит кириллические символы

---

## Использование

Файлы загружаются через `require()` в CommonJS-окружении:

```js
const ru = require('./i18n/ru.json');
const en = require('./i18n/en.json');

function t(key, lang = 'ru') {
  const locale = lang === 'en' ? en : ru;
  return key.split('.').reduce((obj, k) => obj?.[k], locale) ?? key;
}
```

> **Примечание:** Функция `t()` будет реализована в задаче FN-016. JSON-файлы созданы заранее, чтобы можно было разрабатывать и тестировать их независимо.

---

## Тестирование

Тесты находятся в `functions/src/i18n/__tests__/locales.test.js` и используют Vitest.

```bash
cd functions
npx vitest run src/i18n/
```

Тесты покрывают:
- Валидность JSON
- Паритет структуры ключей (рекурсивно)
- Отсутствие пустых значений
- Наличие кириллицы в русском файле
- Наличие обязательных категорий